{
  "build": {
    "builder": "nixpacks",
    "config": {
      "NIXSPACKS_PHP_VERSION": "8.2",
      "NIXPACKS_COMPOSER_ARGS": "--ignore-platform-reqs"
    },
    "postBuildCommand": "mkdir -p /app/database && touch /app/database/database.sqlite && chmod 666 /app/database/database.sqlite && php artisan migrate --force && php artisan cache:clear"
  },
  "deploy": {
    "startCommand": "php artisan serve --host=0.0.0.0 --port=${PORT}"
  }
}